# Minimal tmux configuration for iTerm and terminal-based agents.

set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-256color:RGB"
set -g history-limit 50000
set -sg escape-time 0
set -g focus-events on
set -g mouse on

# Agent launchers may set NO_COLOR for machine-readable tool output. Do not let
# that leak into interactive tmux applications; advertise true color directly.
set-environment -gu NO_COLOR
set-environment -g COLORTERM truecolor

# Keep names under operator control and reflect them in the terminal title.
setw -g allow-rename off
setw -g automatic-rename off
set -g set-titles on
set -g set-titles-string "#S:#W"

# With Caps Lock mapped to Control, Caps Lock+A is the prefix.
unbind C-b
set -g prefix C-a
bind-key a send-prefix

# Reload after editing the installed configuration.
bind-key C-r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded"

# New windows and splits stay in the current working directory.
bind-key c new-window -c "#{pane_current_path}"
bind-key '"' split-window -v -c "#{pane_current_path}"
bind-key % split-window -h -c "#{pane_current_path}"

# Create a visible outer tmux window for a fresh Codex thread. Codex /rename
# becomes its Muster slug and the tmux window name automatically.
bind-key C run-shell 'muster codex window'
